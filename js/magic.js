const person = {
    name: 'chris',
    username: 'chrismean'
};

const MyRandomVar = 'blah';
const num = 1;
let myVar = `blah ${person.name}`;

console.log(object);
/**
 * 
 * @param {*} first 
 * @param {*} second 
 */
function doSomething(first, second){
    return myVar;
}